{
  "name": "zenith-ecosystem",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "zenith": "concurrently \"npm run dev --workspace=apps/zenith-aero\" \"npm run tauri --workspace=apps/zenith-hub dev\" \"npm run dev --workspace=apps/zenith-vigor\" \"npm run dev --workspace=apps/zenith-kratos\" \"npm run dev --workspace=apps/zenith-fuel\" \"npm run dev --workspace=apps/zenith-stride\"",
    "zenith:web": "concurrently \"npm run dev --workspace=apps/zenith-aero\" \"npm run dev --workspace=apps/zenith-hub\" \"npm run dev --workspace=apps/zenith-vigor\" \"npm run dev --workspace=apps/zenith-kratos\" \"npm run dev --workspace=apps/zenith-fuel\" \"npm run dev --workspace=apps/zenith-stride\"",
    "zenith:build": "npm run build:all",
    "build": "npm run build:all",
    "build:all": "npm run aero:build && npm run hub:build && npm run vigor:build && npm run kratos:build && npm run fuel:build && npm run stride:build && node copy-builds.js",
    "aero:dev": "npm run dev --workspace=apps/zenith-aero",
    "aero:build": "npm run build --workspace=apps/zenith-aero",
    "hub:dev": "npm run dev --workspace=apps/zenith-hub",
    "hub:build": "npm run build --workspace=apps/zenith-hub",
    "hub:tauri": "npm run tauri --workspace=apps/zenith-hub",
    "vigor:dev": "npm run dev --workspace=apps/zenith-vigor",
    "vigor:build": "npm run build --workspace=apps/zenith-vigor",
    "kratos:dev": "npm run dev --workspace=apps/zenith-kratos",
    "kratos:build": "npm run build --workspace=apps/zenith-kratos",
    "fuel:dev": "npm run dev --workspace=apps/zenith-fuel",
    "fuel:build": "npm run build --workspace=apps/zenith-fuel",
    "stride:dev": "npm run dev --workspace=apps/zenith-stride",
    "stride:build": "npm run build --workspace=apps/zenith-stride"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "concurrently": "^10.0.4"
  }
}
