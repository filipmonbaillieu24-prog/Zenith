{
  "name": "zenith-ecosystem",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "zenith": "concurrently \"npm run dev --workspace=apps/zenith-aero\" \"npm run tauri --workspace=apps/zenith-hub dev\" \"npm run dev --workspace=apps/zenith-vigor\" \"npm run dev --workspace=apps/zenith-kratos\" \"npm run dev --workspace=apps/zenith-fuel\"",
    "zenith:web": "concurrently \"npm run dev --workspace=apps/zenith-aero\" \"npm run dev --workspace=apps/zenith-hub\" \"npm run dev --workspace=apps/zenith-vigor\" \"npm run dev --workspace=apps/zenith-kratos\" \"npm run dev --workspace=apps/zenith-fuel\"",
    "build": "npm run build:all",
    "build:all": "npm run aero:build && npm run hub:build && npm run vigor:build && npm run kratos:build && npm run fuel:build && node copy-builds.js",
    "aero:dev": "npm --prefix apps/zenith-aero run dev",
    "aero:build": "npm --prefix apps/zenith-aero run build",
    "hub:dev": "npm --prefix apps/zenith-hub run dev",
    "hub:build": "npm --prefix apps/zenith-hub run build",
    "hub:tauri": "npm --prefix apps/zenith-hub run tauri",
    "vigor:dev": "npm --prefix apps/zenith-vigor run dev",
    "vigor:build": "npm --prefix apps/zenith-vigor run build",
    "kratos:dev": "npm --prefix apps/zenith-kratos run dev",
    "kratos:build": "npm --prefix apps/zenith-kratos run build",
    "fuel:dev": "npm --prefix apps/zenith-fuel run dev",
    "fuel:build": "npm --prefix apps/zenith-fuel run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
